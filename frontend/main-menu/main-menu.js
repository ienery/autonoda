'use strict'

import MainMenuWidget from './main-menu-widget';


// $(document).ready(() => {
//     const mainMenuWidget = new MainMenuWidget();
//     mainMenuWidget.render();
// });

$(document).ready(() => {
    const mainMenuWidget = new MainMenuWidget();
    mainMenuWidget.render();
});
